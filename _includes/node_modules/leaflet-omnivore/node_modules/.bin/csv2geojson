#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../csv2geojson/csv2geojson" "$@"
  ret=$?
else 
  node  "$basedir/../csv2geojson/csv2geojson" "$@"
  ret=$?
fi
exit $ret
